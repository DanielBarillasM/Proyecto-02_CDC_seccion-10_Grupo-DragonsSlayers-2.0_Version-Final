// Esperados: SEM003, SEM004, SEM005 y SEM017.
const FIJO: integer = 1;
let cantidad: integer = "diez";
let producto: integer = true * 2;
let logica: boolean = 1 && true;
let mezcla = [1, "dos", true];

FIJO = 2;

if (cantidad) {
  print(cantidad);
}
