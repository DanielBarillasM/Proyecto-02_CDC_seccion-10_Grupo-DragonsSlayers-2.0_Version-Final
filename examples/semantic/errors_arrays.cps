// Esperados: SEM015, SEM016 y SEM017.
let mezcla = [1, "dos"];
let valores: integer[] = [10, 20, 30];
let escalar: integer = 5;

print(valores["cero"]);
print(escalar[0]);

foreach (elemento in escalar) {
  print(elemento);
}
