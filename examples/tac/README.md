# Ejemplos de generación TAC

Los archivos válidos ejercitan expresiones, control de flujo, funciones, arreglos, clases y closures. Los casos de error comprueban que el código intermedio se omite cuando falla una fase anterior.

```bash
npm run cli -- examples/tac/13_programa_integral.cps --mode tac
```
